operator ( a - b )
    return ( a + ( - b ) ) ;

operator ( a ? )
    return if a then 1 else 0 ;

operator ( ! a )
    return ( 1 - ( a ? ) ) ;

operator ( a != b )
    return ( a - b ) ;

operator ( a == b )
    return ( ! ( a != b ) ) ;

operator ( :main )
    let a = 5 ;
    let b = 6 ;
    let c = 3 ;

    return ( ( ( a + c ) - 2 ) == b ) ;
