operator ( a - b )
    return ( a + ( - b ) ) ;

operator ( ! a )
    return ( 1 - a ) ;

operator ( a != b )
    return ( a - b ) ;

operator ( a == b )
    return ( ! ( a != b ) ) ;

operator ( a b -> c )
    return ( ( a + b ) == c ) ;

operator ( :main )
    let a = 7 ;
    let b = 6 ;
    let c = ( a + b ) ;
    let d = ( ( a + 5 ) - b ) ;
    
    return ( ( - d ) c -> a ) ;
